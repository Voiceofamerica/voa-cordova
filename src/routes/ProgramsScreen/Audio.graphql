query ProgramAudio($zone: Int) {
  content(source: zhcn, type: [Article], zoneId: $zone) {
    id
    pubDate
    image {
      tiny
    }
    audio {
      url
      audioTitle
      audioDescription
    }
  }
}
