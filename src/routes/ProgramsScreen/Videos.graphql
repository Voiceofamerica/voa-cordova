query ProgramVideos($zone: Int) {
  content(source: zhcn, type: [Video], zoneId: $zone) {
    id
    pubDate
    video {
      url
      thumbnailTiny
      videoTitle
      videoDescription
    }
  }
}
